<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Accommodation – Philmar Resort</title>
  <link rel="icon" type="image/png" href="/images/logo.png">
  <link rel="stylesheet" href="/css/main.css">
  <style>
    :root {
      --brand: #008080;
      --accent: #FFD700;
      --dark: #222;
      --light: #fff;
      --shadow: rgba(0, 0, 0, 0.15);
      --radius: 12px;
    }

    body {
      font-family: 'Poppins', sans-serif;
      color: var(--dark);
      background: var(--light);
      margin: 0;
      padding: 0;
    }

    /* HERO SECTION */
    .hero {
      position: relative;
      height: 75vh;
      display: flex;
      justify-content: center;
      align-items: center;
      text-align: center;
      color: #fff;
      overflow: hidden;
    }

    .hero img {
      position: absolute;
      top: 0; left: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
      filter: brightness(55%);
      z-index: -1;
    }

    .hero h1 {
      font-size: 3rem;
      font-weight: 600;
      letter-spacing: 2px;
      background: rgba(0, 0, 0, 0.6);
      padding: 1rem 2rem;
      border-radius: var(--radius);
      box-shadow: 0 8px 20px var(--shadow);
      backdrop-filter: blur(3px);
    }

    /* ROOMS SECTION */
    .rooms {
      max-width: 1100px;
      margin: 4rem auto;
      text-align: center;
      padding: 0 1rem;
    }

    .rooms h2 {
      color: var(--brand);
      font-size: 2.5rem;
      margin-bottom: 2rem;
      font-weight: 600;
      position: relative;
    }

    .room-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
      gap: 2rem;
    }

    .room-card {
      background: #fff;
      border-radius: var(--radius);
      box-shadow: 0 4px 15px var(--shadow);
      overflow: hidden;
      transition: 0.3s ease;
    }

    .room-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
    }

    .room-card img {
      width: 100%;
      height: 260px;
      object-fit: cover;
    }

    .room-info {
      padding: 1.2rem;
    }

    .room-info h3 {
      margin: 0;
      font-size: 1.3rem;
      color: var(--dark);
    }

    .room-info p {
      margin-top: 0.5rem;
      color: #555;
      font-size: 0.95rem;
      line-height: 1.5;
    }

    /* FOOTER */
    footer {
      background: var(--dark);
      color: white;
      text-align: center;
      padding: 1.5rem;
      margin-top: 3rem;
      font-size: 0.9rem;
    }

    /* RESPONSIVE */
    @media (max-width: 768px) {
      .hero h1 {
        font-size: 2rem;
        padding: 0.8rem 1.2rem;
      }

      .room-card img {
        height: 200px;
      }
    }
  </style>
</head>

<body>
  <!-- NAVBAR -->
  <%- include('partials/navbar') %>

  <!-- HERO -->
  <section class="hero">
    <img src="/images/room-hero.png" alt="Philmar Resort Accommodation">
    <h1>Relax in Comfort at Philmar Resort</h1>
  </section>

  <!-- ROOMS -->
  <section class="rooms">
    <h2>Our Rooms</h2>
    <div class="room-grid">
      <div class="room-card">
        <img src="/images/room1.jpg" alt="Room 1">
        <div class="room-info">
          <h3>ROOM 1</h3>
          <p>Perfect for couples or solo travelers — modern, cozy, and air-conditioned with a beautiful garden view.</p>
        </div>
      </div>

      <div class="room-card">
        <img src="/images/room2.jpg" alt="Room 2">
        <div class="room-info">
          <h3>ROOM 2</h3>
          <p>Spacious and elegant family suite with a balcony, offering the comfort of home and a touch of luxury.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- FOOTER -->
  <footer>
    <p>&copy; 2025 Philmar Resort. All rights reserved.</p>
  </footer>
</body>
</html>
